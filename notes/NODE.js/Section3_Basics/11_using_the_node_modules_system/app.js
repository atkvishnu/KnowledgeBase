const http = require("http");
const routes = require("./routes"); // custom file  // this file is cached in memory therefore it cannot be changed.


// const server = http.createServer(routes);
const server = http.createServer(routes.handler);


server.listen(3000);