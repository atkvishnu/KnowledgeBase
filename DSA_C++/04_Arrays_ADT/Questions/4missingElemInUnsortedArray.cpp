//Q -> Finding missing elements in an unsorted array.
// Normally this will be a O(n^2) operation if we use nested for loop.
// But here we will use a faster method which utilizes hashing!

// For hashing we create a array H in heap and we initialize it with 0's.

// Steps:
// 1. Take an array of size equal maximum elements 

#include <iostream>
#include <stdlib.h>
#include <stdio.h> 

using namespace std; 

int main()
{
    int A[] = {6,7,8,9,11,12,15,16,17,18,19}; 
    
    return 0;
}
